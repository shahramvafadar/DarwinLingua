namespace DarwinLingua.Learning.Application.Services;

/// <summary>
/// Holds shared constants for the single local-installation user model used in Phase 1.
/// </summary>
internal static class LocalInstallationUser
{
    /// <summary>
    /// Gets the stable user identifier used for local-only Phase 1 persistence.
    /// </summary>
    internal const string UserId = "local-installation-user";
}
